((:somefn 6 7)
 (:somefn (x y)
   (let ([a (new-array x y)])
     (let ([b (new-tuple x y)])
       (let ([c (number? x)])
         (let ([d (number? a)])
           (let ([e (number? b)])
             (let ([c (print c)])
               (let ([d (print d)])
                 (let ([e (print e)])
                   (let ([c (a? x)])
                     (let ([d (a? a)])
                       (let ([e (a? b)])
                         (let ([c (print c)])
                           (let ([d (print d)])
                             (let ([e (print e)])
                               0))))))))))))))))
