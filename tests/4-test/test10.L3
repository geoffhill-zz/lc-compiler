((let ([a -4])
   (let ([b -5])
     (let ([c -6])
       (let ([y (make-closure :t 3)])
         (let ([temp0 (closure-proc y)])
           (temp0 a b c))))))
 (:t (a b c)
  (let ([temp0 (print a)])
    (let ([temp1 (print b)])
      (print c)))))
